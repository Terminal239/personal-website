import Container from "./Reusable/Container";

type Props = {};

const Footer = (props: Props) => {
  return (
    <footer>
      <Container> </Container>
    </footer>
  );
};

export default Footer;
